import 'jest';

// TODO: findIntentsByContext is not yet supported by the provider. Tests should be filled in once provider is update to support it (SERVICE-392?)
describe('Resolving intents by context', () => {
    test.todo('Calling findIntentsByContext with a context type accepted by at least one directory app the promise resolves to an array of all compatible AppIntents');
    test.todo('Calling findIntentsByContext with a context type not accepted by any directory app the promise resolves to an empty array');
});
